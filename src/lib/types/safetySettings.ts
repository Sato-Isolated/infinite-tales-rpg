/**
 * Safety settings types for user-configurable AI content filtering
 * Used to control how strict content filtering is during RPG generation
 */

export type SafetyLevel = 'permissive' | 'balanced' | 'strict';